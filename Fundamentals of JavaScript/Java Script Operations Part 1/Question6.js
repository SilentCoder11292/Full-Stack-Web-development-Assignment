// Q6. Write a Javascript program to calculate the Body Mass Index (BMI) using the formula
//  BMI = weight (kg)/height * height.

let weight = 75; //in kg
let height = 1.67; // in m

let BMI = weight / (height*height);
console.log("BMI =", BMI);